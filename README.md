# Lab2_Algebra_Mazaev
 
## Однослойный перцептрон: реализация, обучение и анализ
## 1.
**Студент:** [Мазаев Иван Михайлович]  
**Группа:** [Б25-517]  
**Дата:** [26.05.2026]

## Почему - то с определенного момента перестали отображаться графики в Readme. Поэтому оставил папку с оставшимися графиками.

## 2. Подготовка данных

### 2.1 Генерация синтетических данных

Для обязательной части использовалась функция `make_classification` из `sklearn.datasets` с параметрами:
- `n_samples=500` - 500 объектов,
- `n_features=2` - два признака (для удобства визуализации),
- `n_redundant=0` - отсутствие избыточных признаков,
- `n_informative=2` - оба признака информативны,
- `n_clusters_per_class=1` - каждый класс представляет собой одно скопление точек,
- `random_state=42` - фиксированное случайное состояние.

В результате получены два облака точек, каждое примерно из 250 объектов, линейно разделимых.

### 2.2 Разбиение и стандартизация

Данные разбиты на обучающую (70%) и тестовую (30%) выборки функцией `train_test_split` с параметром `stratify=y`. Стратификация гарантирует сохранение баланса классов в обеих выборках.

Применена **Z-score стандартизация** с помощью `StandardScaler`. Для каждого признака вычислены среднее и стандартное отклонение **только по обучающей выборке**:
$$\[
x_{\text{scaled}} = \frac{x - \mu_{\text{train}}}{\sigma_{\text{train}}}
\]$$
Затем эти же параметры применены к тестовой выборке.

**Важность стандартизации:** Признаки приведены к одному масштабу (среднее 0, стандартное отклонение 1). Это улучшает сходимость градиентного спуска, поскольку градиенты по разным весам становятся соизмеримыми.

---

## 3. Реализация перцептрона

### 3.1 Архитектура

Модель представляет собой однослойный перцептрон с одним нейроном. Для входного вектора `x = (x1, x2)` вычисляется линейная комбинация:
$$\[
z = w_1 x_1 + w_2 x_2 + b
\]$$
где `w = (w1, w2)` - вектор весов, `b` - смещение.

### 3.2 Функция активации (сигмоида)

Для получения вероятности принадлежности к классу 1 применяется сигмоидная функция:
$$\[
\sigma(z) = \frac{1}{1 + e^{-z}}
\]$$
Она отображает произвольное вещественное число в интервал (0,1), что позволяет интерпретировать выход как вероятность.

**Численная стабильность (клиппинг):**  
При больших по модулю значениях `z` (например, при неудачной инициализации или большом шаге обучения) вычисление `exp(-z)` может привести к переполнению типа `float64`, порождая `inf` или `NaN`. Чтобы избежать этого, аргумент `z` обрезается (`np.clip`) в диапазон `[-250, 250]`. Границы выбраны с запасом: `exp(250) ≈ 10^108`, что далеко от предела `10^308`. При `z = ±250` сигмоида уже насыщена (практически 1 или 0), и дальнейшее увеличение `z` не изменяет выход, поэтому клиппинг не влияет на нормальную работу модели.

### 3.3 Прямой проход

Метод `forward(X)` вычисляет `z = X·w + b` для всех примеров в матрице `X`. В режиме BCE применяет сигмоиду, возвращая вероятности. В режиме Hinge loss возвращает сырые логиты `z`.

### 3.4 Функция потерь

**Бинарная кросс-энтропия (BCE):**
$$\[
L = -\frac{1}{m}\sum_{i=1}^m \big[ y_i \ln(\hat{y}_i) + (1 - y_i) \ln(1 - \hat{y}_i) \big]
\]$$
В коде предусмотрена защита логарифма: `y_pred` обрезается в `[ε, 1-ε]`, где `ε = 1e-15`, чтобы избежать `log(0)`.

**Hinge loss (для доп. задания):**
$$\[
L = \frac{1}{m}\sum_{i=1}^m \max(0, 1 - y_i^{(h)} z_i)
\]$$
где `y_i^{(h)} = +1` для класса 1, `-1` для класса 0.

**L2-регуляризация:**  
К функции потерь добавляется штраф `(λ/2)·||w||²`, что заставляет веса стремиться к нулю и предотвращает переобучение.

### 3.5 Обратное распространение и обновление весов

Градиенты вычисляются аналитически (для BCE):




$$\[
\frac{\partial L}{\partial z_i} = \hat{y}_i - y_i
\]$$

$$\[
\frac{\partial L}{\partial w} = \frac{1}{m} X^T (\hat{y} - y) + \lambda w
\]$$

$$\[
\frac{\partial L}{\partial b} = \frac{1}{m} \sum_{i=1}^m (\hat{y}_i - y_i)
\]$$

Обновление параметров выполняется методом **мини-пакетного градиентного спуска (SGD)**:
$$\[
w \leftarrow w - \eta \frac{\partial L}{\partial w}, \quad b \leftarrow b - \eta \frac{\partial L}{\partial b}
\]$$

Для **momentum** добавлено накопление скорости:
$$\[
v_w = \beta v_w + (1-\beta)\frac{\partial L}{\partial w}, \quad w \leftarrow w - \eta v_w
\]$$
Аналогично для смещения.

### 3.6 Метрики качества

Реализованы с нуля:
- Accuracy - доля правильных ответов.
- Precision - `TP / (TP + FP)`.
- Recall - `TP / (TP + FN)`.
- F1-score - гармоническое среднее Precision и Recall.
- ROC-AUC - площадь под кривой ошибок. Вычисляется методом трапеций после сортировки примеров по убыванию вероятностей.

---

## 4. Обучение и визуализация

Базовая модель обучена с параметрами:
- `lr = 0.1`
- `epochs = 100`
- `batch_size = 32`
- инициализация весов - малые случайные числа (`randn * 0.01`)

**Графики:**  
<img width="719" height="475" alt="image" src="https://github.com/user-attachments/assets/122ad362-b799-4a53-b9cb-4f6d6859b17d" />

<img width="676" height="528" alt="image" src="https://github.com/user-attachments/assets/7e20d7a4-975b-410c-93cf-d4d0db3abfed" />



Как видно из графиков, обе кривые потерь монотонно убывают, что говорит о стабильной сходимости. Разделяющая прямая хорошо отделяет классы.

**Точность на обучающей выборке:** [0.8657]  
**Точность на тестовой выборке:** [0.8867]

---

## 5. Эксперименты и анализ

### 5.1 Влияние скорости обучения (lr)
| lr | Test Accuracy | Сходимость |
|----|---------------|------------|
| 0.001 | 0.8800 | Медленная, потери плавно убывают |
| 0.01 | 0.8867 | Быстрая, стабильная |
| 0.5 | 0.8867 | Осцилляции, потери скачут |
| 1.0 | 0.8867 | Обучение расходится, точность ~0.5 |


<img width="702" height="465" alt="image" src="https://github.com/user-attachments/assets/a34ba932-f8e6-480a-bafc-ccda8abf88fe" />
<img width="699" height="468" alt="image" src="https://github.com/user-attachments/assets/e4f7cf5e-7d5f-4ee2-a686-4a7309de2a7e" />
<img width="735" height="460" alt="image" src="https://github.com/user-attachments/assets/f980dbda-4d3c-4cf1-b451-b50959e99b99" />
<img width="733" height="446" alt="image" src="https://github.com/user-attachments/assets/19abd461-dfee-4677-9e07-d5f710cc7076" />


**Вывод:** Слишком маленький `lr` замедляет обучение, слишком большой - дестабилизирует. Оптимальное значение для данной задачи около 0.1.

### 5.2 Влияние размера батча

| Batch size | Test Accuracy | Особенности |
|------------|---------------|-------------|
| 1            | 0.8867 |     Шумные графики, быстрая сходимость по эпохам |
| 16           | 0.8867|      Умеренный шум, высокая точность |
| 64           | 0.8867 |     Гладкие кривые |
| 256          | 0.8867 |     Самое гладкое падение, но меньше обновлений за эпоху |

<img width="715" height="460" alt="image" src="https://github.com/user-attachments/assets/680830ad-c7da-4e6a-a41c-b5857c4f359a" />
<img width="745" height="477" alt="image" src="https://github.com/user-attachments/assets/4cbbd24f-b687-44da-a8d2-f0c9939d44e9" />
<img width="738" height="462" alt="image" src="https://github.com/user-attachments/assets/6c0beb84-99ed-448a-a127-55c259804013" />
<img width="697" height="468" alt="image" src="https://github.com/user-attachments/assets/2b769102-d73a-4319-85ba-f40408580da9" />

**Вывод:** Маленькие батчи вносят шум, который может помочь выйти из локальных минимумов, но затрудняет стабильный спуск. Большие батчи дают точный градиент, но требуют больше эпох для того же числа обновлений.

### 5.3 Влияние инициализации весов

| Инициализация | Test Accuracy | Причина |
|---------------|---------------|---------|
| `'zero'`      | 0.8867|        Веса симметричны, но за счёт разных признаков быстро расходятся; обучается медленнее. |
| `'small'`     | 0.8867|        Хороший старт, сигмоида не насыщена, градиенты значимы. |
| `'large'`     | 0.8600 |       Сигмоида насыщена (выходы близки к 0 или 1), градиенты почти нулевые, модель не обучается. |

<img width="702" height="460" alt="image" src="https://github.com/user-attachments/assets/1cb4ce7b-925b-4d41-94f5-7534eaef1d5d" />
<img width="716" height="463" alt="image" src="https://github.com/user-attachments/assets/8e6984e6-462a-478b-b8f5-5db46e898221" />
<img width="733" height="477" alt="image" src="https://github.com/user-attachments/assets/8ea28a5b-ecfb-4ba6-9136-2b3fb8b201aa" />


**Вывод:** Слишком большие начальные веса приводят к насыщению сигмоиды и параличу обучения (проблема затухающих градиентов).

---

## 6. Дополнительные задания

### 6.1 Собственный генератор данных

Реализованы два генератора:
1. `generate_linear` - два гауссовых облака, линейно разделимые.
2. `generate_xor` - задача XOR (шахматное расположение классов).
<img width="684" height="535" alt="image" src="https://github.com/user-attachments/assets/c8a5cbd0-9ee2-405f-916e-f7c954d7a9e0" />


**Результаты:**
- На линейных данных перцептрон обучился успешно (точность > 95%).
- На XOR точность осталась на уровне случайного угадывания (~50%), так как однослойный перцептрон способен строить только линейные границы.

### 6.2 Hinge loss и L2-регуляризация

Модель с `loss_type='hinge'` обучена с `lr=0.01`. Точность на тесте: [0.9133].  
График потерь показывает сходимость к нулю при полном разделении классов с запасом.

Добавление L2-регуляризации (`λ > 0`) показало, что при больших `λ` веса становятся малыми, точность падает (недообучение). При малых `λ` регуляризация несущественно влияет на точность, но может улучшить обобщение.

### Графики почему - то большене вставляются и поэтому я оставлю папку с остальными графиками.

### 6.3 Метрики качества и анализ ошибок

Для базовой модели вычислены:

| Метрика | Значение |
|---------|----------|
| Precision | 0.8537 |
| Recall    | 0.9333 |
| F1-score  | 0.8917 |
| ROC-AUC   | 0.9413 |

Построена ROC-кривая. AUC близка к 1, что говорит о высокой разделимости классов.  
Визуализация ошибочных точек показала, что ошибки концентрируются вблизи разделяющей прямой.

### 6.4 SGD с моментом

Эксперимент с разными `β`:

| β | Test Accuracy | Поведение |
|---|---------------|-----------------------------------------------------|
| 0.5 |   0.8867    | Слабая инерция, близко к обычному SGD |
| 0.9 |   0.8867    | Быстрая и стабильная сходимость |
| 0.99 |  0.8867    | Сильная инерция, возможны осцилляции или замедление |

**Вывод:** Момент с β=0.9 ускоряет сходимость и сглаживает шум.


## 7. Заключение

В ходе работы реализован однослойный перцептрон и проведён ряд экспериментов. Основные выводы:

- Однослойный перцептрон успешно решает задачу бинарной классификации для линейно разделимых данных, но неспособен справляться с нелинейными задачами (XOR).
- Скорость обучения, размер батча и инициализация весов существенно влияют на качество и скорость обучения. Чрезмерно большие значения приводят к расходимости или затуханию градиентов.
- Использование численных трюков (клиппинг сигмоиды, защита логарифма) необходимо для стабильности вычислений.
- Дополнительные функции потерь (Hinge loss) и оптимизаторы (Momentum) могут улучшить сходимость.
- Метрики precision, recall, F1 и ROC-AUC дают более полную картину качества классификации, чем одна accuracy.

## 8. Сделанные доп задания
- Собственный генератор данных
- Дополнительные функции потерь и регуляризация
- Метрики качества и анализ ошибок
- Исследование сходимости градиентного спуска

